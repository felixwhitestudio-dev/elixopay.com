# Production Environment Variables for Railway.app
# Copy these to Railway Dashboard > Variables

NODE_ENV=production
PORT=3000
API_VERSION=v1

# Database Configuration (Railway PostgreSQL)
DB_HOST=${PGHOST}
DB_PORT=${PGPORT}
DB_NAME=${PGDATABASE}
DB_USER=${PGUSER}
DB_PASSWORD=${PGPASSWORD}

# JWT Secret (Generate secure random string)
JWT_SECRET=${JWT_SECRET}
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
JWT_REFRESH_EXPIRES_IN=30d

# Frontend URL (Update with your Vercel/Railway domain)
FRONTEND_URL=https://elixopay.vercel.app
SERVER_URL=https://elixopay-production.up.railway.app

# Redis (if using Railway Redis)
REDIS_URL=${REDIS_URL}

# Security
BCRYPT_ROUNDS=10

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Email (Optional - configure later)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=${SMTP_USER}
SMTP_PASSWORD=${SMTP_PASSWORD}
EMAIL_FROM=noreply@elixopay.com

# Logging
LOG_LEVEL=info
